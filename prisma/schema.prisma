datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}

generator client {
    provider = "prisma-client-js"
}

model OtelColConfig {
    id        String   @id @default(cuid())
    updatedAt DateTime @updatedAt
    name      String
    config    String
}
