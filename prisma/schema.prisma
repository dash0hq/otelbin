datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
    relationMode = "prisma"
}

generator client {
    provider = "prisma-client-js"
}

model OtelColConfig {
    id        String   @id @default(cuid())
    updatedAt DateTime @updatedAt
    name      String
    config    String
}
